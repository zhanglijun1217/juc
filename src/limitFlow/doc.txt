常见的限流算法
    令牌桶、漏桶、计数器等。

    1.令牌桶
    令牌桶算法的原理是系统以一个恒定的速度往桶里放令牌，而如果请求需要被处理，则需要先从桶里获取一个令牌，当桶里没有令牌可取时，则拒绝服务。
    2.漏桶
    漏桶算法思路很简单，水（请求）先进入到漏桶中，漏桶以一定的速度出水，当水流入速度过大则直接溢出，可以看出漏桶算法能强行限制数据的传输速率。
    3.计数器限流算法
    计数器限流算法也是比较常用的，主要用来限制总并发数，比如数据库连接池大小、线程池大小、程序访问并发数等。

